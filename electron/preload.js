'use strict';
// Intentionally empty: the renderer just loads the app's own HTTP origin
// (http://127.0.0.1:<port> in production, http://localhost:3005 in dev) and
// talks to it exactly like a browser tab would — no privileged IPC surface
// needed.
